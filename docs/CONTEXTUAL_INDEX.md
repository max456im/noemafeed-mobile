# 🕰️ Контекстуальный индекс новостей 

> **Версия**: 1.0  
> **Лицензия**: GPL-3.0-or-later  
> **Статус**: опциональный модуль для `noemafeed-mobile`  

---

## 🎯 Назначение

**Контекстуальный индекс** — это онтологически структурированная база исторических событий, позволяющая NoemaFeed:

- отличать **повторяющиеся паттерны** от уникальных возмущений,  
- избегать **онтологической амнезии** («этого раньше не было»),  
- определять **фазу онтогенеза** системы (первый кризис или циклический повтор?),  
- связывать текущее событие с **причинно-временными цепочками**.

Он превращает новость из изолированного факта в **элемент культурно-исторического процесса**.

---

## 🧱 Структура данных

Индекс реализован как **Temporal Knowledge Graph (TKG)** в формате RDF/Turtle и хранится в файле:

```
kb/dynamics/context-index.ttl
```

### Основные типы сущностей:
- `news:Event` — событие (например, «Запрет ИИ в школах, Франция, 2026»)
- `actor:Organization`, `actor:Person` — акторы
- `pert:PerturbationType` — тип возмущения (из Базы 14)

### Ключевые онтологические отношения:

| Свойство | Значение |
|--------|--------|
| `temporal:ledTo` | Прямая причинно-следственная связь |
| `temporal:analogousTo` | События схожей структуры (разные эпохи, один паттерн) |
| `temporal:partOfCycle` | Входит в повторяющийся цикл (например, «кризис доверия к технологиям») |
| `temporal:triggeredBy` | Непосредственный триггер (скандал, утечка, закон) |

### Пример записи:
```turtle
:event2026_fr_ai_ban a news:Event ;
    rdfs:label "Запрет ИИ в школах"@ru ;
    temporal:occurredAt "2026-02-06"^^xsd:date ;
    temporal:hasActor :FranceMinistryOfEducation ;
    temporal:describesAction :BanOnAIInSchools ;
    temporal:analogousTo :event1970_us_calculator_ban ;
    temporal:partOfCycle pert:TechnologicalMoralPanic .

:event1970_us_calculator_ban a news:Event ;
    rdfs:label "Запрет калькуляторов в школах"@en ;
    temporal:occurredAt "1970-09-01"^^xsd:date ;
    temporal:hasActor :US_SchoolBoard ;
    temporal:describesAction :BanOnCalculators .
```

---

## 🌐 Источники данных

Все данные получены из открытых источников и преобразованы в онтологический формат:

- **GDELT Project** — глобальные события, тональность, акторы (CC BY-SA 4.0)  
- **Wikidata Events** — структурированные исторические факты (CC0)  
- **DBpedia** — связи между организациями, законами, технологиями (CC BY-SA)  
- **Архивы СМИ** — через onto16-парсеры (только при наличии лицензии на использование)

> ⚠️ **Важно**: NoemaFeed **не парсит сайты в реальном времени**. Все данные предварительно обработаны и включены в локальный граф.

---

## 🔍 Как используется в NoemaFeed Mobile

1. При анализе новости система ищет:
   - аналогичные события (`temporal:analogousTo`),
   - предшествующие триггеры (`temporal:triggeredBy`),
   - принадлежность к циклу (`temporal:partOfCycle`).

2. Если найдено совпадение:
   - В режиме **🔵 Аналитический** показывается:  
     > «Это напоминает запрет калькуляторов в 1970-х: страх перед новым инструментом».
   - В режиме **🔴 Критический**:  
     > «Цикл “технологическая моральная паника” повторяется каждые 10–15 лет».

3. Если аналогов нет — событие помечается как **потенциально уникальное** → требует особой осторожности.

---

## 📦 Подключение в мобильной версии

Контекстуальный индекс **не входит в минимальную сборку** по умолчанию, чтобы сохранить небольшой размер PWA (~5 МБ).

### Как включить:
1. Скачайте `context-index.ttl` из релизов GitHub.
2. Поместите его в папку `kb/dynamics/` (в Android — через файловый менеджер).
3. Перезапустите приложение → появится опция **«Расширенный контекст»** в настройках.

> 💡 Альтернатива: используйте **ZIM-архив Википедии** — приложение автоматически найдёт статьи по ключевым словам и предложит их как контекст.

---

## 🛡️ Этические ограничения

- Индекс **не содержит** персональных данных живых людей без согласия.
- Все исторические аналогии **проверяются на онтологическую корректность** (например, нельзя сравнивать Холокост с технозапретами).
- При обнаружении **травматического события** активируется фильтр из Базы 15 (коллективная память).

---

## 📚 Дополнительно

- См. также: [`COLLECTIVE_MEMORY.md`](COLLECTIVE_MEMORY.md) — как культурная память ограничивает интерпретацию.
- Техническая спецификация: [KB_SPEC.md](KB_SPEC.md)

---

> **NoemaFeed не просто знает, что было. Он помнит, почему это важно.**