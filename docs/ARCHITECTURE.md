# 🏗️ Архитектура NoemaFeed Mobile

NoemaFeed Mobile — это **онтологически автономный новостной агент**, построенный не на алгоритмах машинного обучения, а на **формализованной системе устойчивости** — **Формуле Устойчивого Существования (ФУС)**.  
Архитектура разделена на три слоя: **ядро**, **базы знаний** и **интерфейс**, каждый из которых работает **локально и оффлайн**.

---

## 🔷 1. Ядро: ФУС-процессор (Rust → WASM)

### Назначение
Преобразует событие (новость) в **онтосцену** — структурированное описание возмущения через призму пяти уровней бытия и трёх нормативных слоёв.

### Компоненты
- **ФУС-диагностик**: определяет, какие инварианты нарушены (Различение, Агентность, Свобода-в-детерминизме и др.).
- **Субъект-идентификатор**: сопоставляет акторов с профилем пользователя (темперамент, зодиак, элемент).
- **Контекст-анализатор**: связывает событие с историческими аналогами (через Wikidata/GDELT).
- **Нормативный фильтр**: отсекает недопустимые интерпретации (GPL, GDPR, этические рамки).
- **Реконструктивный генератор**: формирует вывод в одном из трёх режимов:
  - 🟢 **Конструктивный** — усиление созидательного,
  - 🔵 **Аналитический** — ретро/проспективный взгляд,
  - 🔴 **Критический** — разрушение ради сохранения целого.

### Технологии
- Написано на **Rust**.
- Компилируется в **WebAssembly** через `wasm-bindgen`.
- Использует `oxigraph::Store` для хранения и запроса RDF-графов.
- Полностью **детерминировано** — без случайности, без LLM, без облака.

---

## 📚 2. Базы знаний: минимальный набор (10 баз)

Все базы хранятся локально в формате **Turtle/JSON-LD** и загружаются при старте.

| Группа | Базы | Назначение |
|--------|------|-----------|
| **Ядро ФУС** | `core.ttl`, `structural.ttl`, `archetypal.ttl`, `identity.ttl`, `existential.ttl` | Инварианты 5 уровней бытия |
| **Профили** | `astro-profiles.ttl`, `subject-registry.ttl` | Темперамент по дате рождения, типы субъектов |
| **Динамика** | `perturbations.ttl`, `ontogenesis.ttl`, `context-index.ttl` | Типы возмущений, фазы реконструкции, исторический контекст |

> Эти 10 баз — **полностью открытые** (GPL-3.0+) и достаточны для полноценной работы.

### Опциональные расширения
- **Коллективная память** (`collective-memory.ttl`) — травмы, каноны (см. `COLLECTIVE_MEMORY.md`).
- **Расширенный контекст** — через ZIM-архив Википедии (поддерживается, но не обязателен).

---

## 🌐 3. Интерфейс: PWA (Progressive Web App)

### Принципы
- **Оффлайн-first**: после первого запуска работает без интернета.
- **Без аккаунтов, без трекеров, без рекламы**.
- **Минимальный UX**: текущая новость +上下сниппеты + кнопки режимов.

### Особенности
- Поддерживает **установку на Android/iOS** как нативное приложение.
- Все данные хранятся в **IndexedDB** или `localStorage`.
- Полный текст новости загружается **только по запросу** (кнопка «Показать полный анализ»).
- Анализ можно **копировать в буфер** или **делиться онтологическим дайджестом**.

---

## 🧩 4. Онтологическая модель: матрица 5×3

Ядро реализует **матрицу из 5 уровней бытия × 3 нормативных слоя**:

| Уровень бытия | Структура (юридика) | Память (практики) | Выражение (ценности) |
|---------------|---------------------|-------------------|-----------------------|
| Онтологический | GPL, право на автономию | OAIS, верификация | «Субъект не может быть имитирован» |
| Физический     | Техрегламенты       | Инженерные стандарты | «Энергия не тратится впустую» |
| Биологический  | Биобезопасность     | Медицинские протоколы | «Тело священно» |
| Психический    | GDPR, право на объяснение | Образование, ритуалы | «Я — автор своей жизни» |
| Экзистенциальный | UN Charter, IEEE Ethics | Коллективные травмы | «Человек — цель, а не средство» |

Эта матрица — **не метафора**, а **рабочая схема диагностики и реконструкции**.

---

## ⚙️ 5. Техническая сборка

```bash
git clone https://github.com/max456im/noemafeed-mobile.git
cd noemafeed-mobile
wasm-pack build --target web
# Откройте web/index.html